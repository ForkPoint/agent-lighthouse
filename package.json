{
  "name": "agent-lighthouse-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Lighthouse-style audits for AI agents, LLM crawlers, MCP clients, and the agentic web",
  "author": "ForkPoint",
  "license": "Apache-2.0",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "pnpm -r run typecheck",
    "lint": "oxlint .",
    "check:dossiers": "node scripts/check-dossiers.mjs",
    "check:requires": "node scripts/check-requires.mjs",
    "format": "prettier --write \"**/*.{ts,tsx,js,json,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,json,md}\"",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "pnpm build && changeset publish",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.12",
    "@types/node": "^22.10.5",
    "@vitest/coverage-v8": "2.1.9",
    "oxlint": "^0.15.10",
    "prettier": "^3.4.2",
    "tsup": "^8.3.5",
    "tsx": "^4.23.12",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  },
  "packageManager": "pnpm@9.15.4"
}
