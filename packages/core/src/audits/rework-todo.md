# Redemption rework — TODO

Audits that stayed in the framework **only if rewritten**. Every row below is now resolved: each source file carried a `TODO(redeem)` header with its required rework, all of them were executed against the linked dossier, and no `TODO(redeem)` marker remains in the registry. This file is kept as the record of what each rewrite had to achieve and what landed. Companion lists: [proposed new audits](./proposed/README.md) · [deletion research](../../../../docs/evidence/deletions/README.md).

## Approved by deletion review (2026-08-21) — 9

- [x] DONE `agent-interfaces/ai-catalog-exists` — rewritten to ARD §4.1 2026-08-22 (Plan 4, Task 10) · [dossier](../../../../docs/evidence/audits/agent-interfaces/ai-catalog-exists.md) · [research](../../../../docs/evidence/deletions/agent-tools/ai-catalog-exists.md)
  - Grade A, tier `scored`, weight 1.0 — unchanged. Landed as specified: pass condition is `specVersion` + `host` + a non-empty `entries[]`, the invented `services` array is gone, and the guidance/code sample carry the real ARD schema. Shared parsing extracted to `_ard.ts`.
- [x] DONE `agent-interfaces/ai-catalog-metadata` — rewritten to ARD §4.2 metadata 2026-08-22 (Plan 4, Task 10) · [dossier](../../../../docs/evidence/audits/agent-interfaces/ai-catalog-metadata.md) · [research](../../../../docs/evidence/deletions/agent-tools/ai-catalog-metadata.md)
  - Grade B, tier `scored`, weight 0.6 — unchanged. Scores `host.displayName` plus the entry keys hf-discover indexes (`description`, `tags`, `capabilities`, `representativeQueries`), with entry-level `updatedAt` and entry/host-level `trustManifest` as reported bonuses. `owner`/`contact`/`lastUpdated`/`services` dropped; absence is now `na`, not a second zero for the same missing file.
- [x] DONE `agent-interfaces/ai-catalog-urls` — rewritten to `entries[].url` liveness 2026-08-22 (Plan 4, Task 10) · [dossier](../../../../docs/evidence/audits/agent-interfaces/ai-catalog-urls.md) · [research](../../../../docs/evidence/deletions/agent-tools/ai-catalog-urls.md)
  - Grade B, tier `scored`, weight 0.6 — unchanged. Probes `entries[].url`, skips inline `data` entries, accepts auth-gated 401/403/405/429 as reachable, `isSafeUrl()`-gated, concurrency capped at 5, absence is `na`.
- [x] DONE `agent-interfaces/ai-catalog-link` (4.19) — redeemed by merge into `agent-interfaces/ai-catalog-exists` 2026-08-22 (Plan 4, Task 7) · [merged dossier](../../../../docs/evidence/audits/agent-interfaces/ai-catalog-exists.md) · [source dossier](../../../../docs/evidence/merged/agent-interfaces/ai-catalog-link.md)
  - Grade B: the mechanism is written into two draft specs (ARD §6.1 and the LF Agent Card WG consuming guide) and is genuinely deployed in production with the exact rel token, verified by live fetch of neon.com and specification.website. That clears the bar for keeping the check. Landed as specified: `rel="ai-catalog"` matched as a token (any type) plus the HTTP `Link` header, across all pages, downgraded to a nice-to-have that turns "no catalog" into a warn rather than passing on its own.
- [x] DONE `agent-interfaces/webmcp-declarative-forms` — rewritten to the Baseline `declarative-webmcp` attributes 2026-08-22 (Plan 4, Task 10) · [dossier](../../../../docs/evidence/audits/agent-interfaces/webmcp-declarative-forms.md) · [research](../../../../docs/evidence/deletions/agent-tools/webmcp-declarative-forms.md)
  - Grade A, tier `scored`, weight 1.0 — unchanged. All three recommended fixes landed: dead `webmcp.link` docsUrl replaced with the Chrome declarative-API docs, a non-empty `toolname` required to count a form, `defaultPriority` softened `high` → `medium`. Form-less pages are `na`.
- [x] DONE `structured-data/speakable-schema` — gated to news/article publishers 2026-08-22 (Plan 4, Task 12) · [dossier](../../../../docs/evidence/audits/structured-data/speakable-schema.md) · [research](../../../../docs/evidence/deletions/structured-data/speakable-schema.md)
  - Grade A: a live vendor doc names a specific agent (Google Assistant) that reads the signal, and the feature is still listed in Google's current supported-features gallery, so the rubric mandates 'redeemable'. But it must be redeemed in narrowed form, not as-is: (a) applicability should be restricted to news/article publishers (the audit currently runs site-wide with no page-type gate and defaults to fail for every non-news site), and (b) the description's claim that Alexa and Siri consume speakable must be deleted — it is unsupported by any vendor doc and directly contradicted by Applebot's documentation, which lists isAccessibleForFree as its only schema.org property.
  - Grade A, tier `scored`, weight 1.0 — unchanged. Both required changes landed: (a) `applicablePageTypes: ['content']` keeps `planAudits()` from running the audit at all on a crawl with no content page, and the runtime guard assesses only article pages (`pageType === 'content'` or a node carrying Article/NewsArticle/BlogPosting), returning `na` otherwise — non-news sites no longer take a hard 0 on a news-only signal; (b) Alexa and Siri are gone from the description, impact, fix, code sample and failure copy, with a regression test pinning it, and `docsUrl` moves to the Google Search Central speakable doc that carries the consumer statement. The code review's false positives are fixed alongside: a single-string `cssSelector` and the `xpath` alternative are accepted, empty selectors are not, `speakable` must sit on an Article/WebPage-family node, and coverage is judged per article page ("N of M") so `scoreDisplayMode` moves `binary` → `ternary`. The Gemini-for-Home retirement is stamped as a re-check trigger in the source header and the dossier.
- [x] DONE `access-crawl-control/sensitive-paths` — rewritten to RFC 9309 crawl hygiene 2026-08-22 (Plan 4, Task 11) · [dossier](../../../../docs/evidence/audits/access-crawl-control/sensitive-paths.md) · [research](../../../../docs/evidence/deletions/crawler-permissions/sensitive-paths.md)
  - Grade A on the mechanism: named AI crawlers are documented to honor path-level Disallow, with literal directory examples from Apple (Applebot and the AI-training token Applebot-Extended, 'Disallow: /private/') and Meta (meta-externalagent, 'Disallow: /private/ # Disallow a specific directory'), on top of the ratified RFC 9309 path-matching semantics that OpenAI and Anthropic both point publishers to. Per the rubric that makes it redeemable — but it needs surgery, not preservation as written.
  - Grade A, tier `scored`, weight 1.0 — unchanged. All four required changes landed: (a) security/privacy framing dropped, guidance now carries RFC 9309's "not a substitute for valid content security measures" and the audit is reframed as crawl hygiene; (b) `/api/` removed outright, not made opt-in; (c) the ChatGPT-User / Perplexity-User exemptions are stated in the guidance; (d) `defaultPriority` `high` → `low`. Candidate paths are now discovered from the crawl (page URLs, same-origin links, sitemap `<loc>`) instead of hardcoded, absence of any low-value family is `na`, matching reuses `isPathAllowed` from `gatherers/robots.ts`, coverage is judged across six documented AI crawler tokens, and a blanket-blocked site is `na` so it no longer contradicts `no-blanket-block`.
- [x] DONE `content-extraction/aside-element` — rewritten to a conditional extraction check 2026-08-22 (Plan 4, Task 11) · [dossier](../../../../docs/evidence/audits/content-extraction/aside-element.md) · [research](../../../../docs/evidence/deletions/semantic-html/aside-element.md)
  - Grade B => redeemable, and the audit's stated mechanism is essentially verbatim correct. Which consumer reads the signal, and where documented: (a) Mozilla Readability removes `<aside>` via `this._clean(articleContent, "aside")` in Readability.js — the extractor behind Firefox Reader Mode, Jina Reader's readability path, and a long tail of LLM/agent tools; (b) trafilatura removes `<aside>` via its MANUALLY_CLEANED list in trafilatura/settings.py — a standard extractor in LLM corpus pipelines; (c) Chromium exposes `<aside>` as a `complementary` landmark in the accessibility tree that Anthropic's browser use tool returns from `read_page`, which I verified directly by snapshotting a probe page.
  - Grade B, tier `scored`, weight 0.6 — unchanged. The check is now conditional: supplementary blocks are detected by class/id on content pages, `pass` when every one is inside `<aside>`/`role="complementary"`, `warn` when some are, `fail` when none are (the div-based-sidebar case the audit always claimed to catch and never could), and `na` when a page legitimately has no supplementary content. The page loop is filtered to `pageType === 'content'` to match `applicablePageTypes`, `scoreDisplayMode` moves `binary` → `ternary`, and the guidance now states the extraction consequence — Readability's `_clean(articleContent, "aside")` and trafilatura's `MANUALLY_CLEANED` discard the subtree, so citable facts must never live in an `<aside>`.
- [x] DONE `answer-readiness/trust-signals` — rebuilt on the GEO-benchmark factors 2026-08-22 (Plan 4, Task 11) · [dossier](../../../../docs/evidence/audits/answer-readiness/trust-signals.md) · [research](../../../../docs/evidence/deletions/generative-engine/trust-signals.md)
  - Grade B: there is strong, quantified, multi-model empirical data that trust and social-proof cues in retrieved page text change which source an AI answer engine cites — 252,000 controlled trials, 4-5 of 6 models significant, plus a +17% 'Authoritative' effect in the KDD'24 GEO benchmark. Per the rubric that makes it redeemable, and unlike the other three audits here it has a real measured mechanism behind it.
  - Grade B, tier `scored`, weight 0.6 — unchanged. Re-specified to the three factors arXiv 2605.25517 measured: quantified social proof (a rating, review count or customer count — always with a magnitude), evidence-backed claims (≥2 outbound citation hosts or `<cite>`/`blockquote[cite]`), and comparison content (a `<th>`-bearing table or a comparison-framed heading); pass at ≥2 of 3, warn at 1, fail at 0. All promotional-puffery patterns deleted along with the untested rest of the list and the `[class*="logo"]` image-grid heuristic. Non-double-counting is structural: `findReviewNodes()` is imported from `review-signals.ts` and the social-proof factor is _deferred_ to that audit when review markup is present, while `external-citations` and `comparison-tables` are scoped to non-homepage page types. Scoped to `applicablePageTypes: ['homepage']` and selected by page type rather than `pages[0]`; a non-English homepage is `na` instead of a confident fail. The "smaller gains" demotion the note asks for is carried by the grade (0.6 against a grade-A gatekeeper's 1.0, since the §4 law forbids hand-setting a weight) plus `defaultPriority` `medium` → `low`.

## Proposed by first triage — all resolved — 14

Six of these were reached in Plan 4 and initially skipped, because the user's first 2026-08-22 decision was that pending-triage redeems were not approved. The user approved all six later the same day and they were executed in Task 16; every row below is now `DONE`. Three of them landed at a lower tier than their row proposed (`direct-definitions`, `cors-api-routes` and, in the earlier wave, `openapi-link`): a `scored` target is unreachable for a grade-C audit, because `weightForGrade('C', 'scored') = 0` while `sunset.test.ts` enforces `tier !== 'scored' ⟺ weight === 0`. Each row records the decision. The stale `content-discoverability/mobile-friendly` row was removed on the same date — that audit was sunset in Plan 3, so there was nothing left to rework.

**Nothing in this file is outstanding.** Every audit the evidence review flagged for rework has been rewritten against its dossier.

- [x] DONE `operability-safety/form-error-messages` — rebuilt as a validation-linkage coverage ratio 2026-08-22 (Plan 4, Task 16) · [dossier](../../../../docs/evidence/audits/operability-safety/form-error-messages.md)
  - Rebuild: verify aria-describedby/aria-errormessage linkage on invalid-state inputs instead of current broken heuristic. Evidence: a11y-tree consumption by computer-use agents graded A..
  - Grade A, tier `scored`, weight 1.0 — target tier met. Landed as specified: `aria-invalid="true"` fields are the assessed population when any exist, otherwise `required`/`aria-required` fields, and a crawl with neither is `na`. `aria-errormessage` is accepted on equal terms with `aria-describedby`, so the ARIA 1.2 wiring the old audit warned about now passes. The vacuous first-match pass is gone (`1 of 240` warns and says so), fields outside `<form>` are counted, id resolution moved to a `Set` so selector metacharacters cannot break it, and `pageUrl` points at the first offending page. Title and description no longer claim to observe live error messages, which a GET cannot.
- [x] DONE `agent-interfaces/webmcp-registered-tools` — manifest check replaced by runtime registered-tools detection 2026-08-22 (Plan 4, Task 16) · [dossier](../../../../docs/evidence/audits/agent-interfaces/webmcp-registered-tools.md)
  - Evidence reshape: the .well-known manifest file is invented (grade D) — but runtime-registered WebMCP tools are grade B: Google Lighthouse 13.3+ ships 'Registered WebMCP tools' audits in its new Agentic Browsing category. Replace manifest-file audit with registered-tools detection, experimental tier..
  - Grade **A → B**, tier `experimental`, weight 0 — target tier met, grade restated to the one the redemption note assigns the surviving mechanism (the dossier's A belonged to the `agent-surface-soft-404-validation` meta-signal, which no longer applies now that the well-known path is gone). Landed as specified: `/.well-known/webmcp` is deleted from the audit and from the orchestrator's `rootFilePaths` (this audit was its only reader), and tools are read from `navigator.modelContext.registerTool` / `provideContext` in inline scripts. Absence is `na` with the JS-runtime limitation stated in the message rather than a universal hard fail, declarative-only sites defer to `webmcp-declarative-forms`, and `defaultPriority` drops `high` → `low`. Class renamed `WebmcpManifestAudit` → `WebmcpRegisteredToolsAudit`.
- [x] DONE `access-crawl-control/ai-content-declaration` — rewritten to the real directive names 2026-08-22 (Plan 4, Task 16) · [dossier](../../../../docs/evidence/audits/access-crawl-control/ai-content-declaration.md)
  - Evidence upgrade from delete: noai/noimageai/tdm-reservation declaration meta tags graded D/experimental — real emerging opt-out ecosystem, no ratified consumer yet. Experimental, unscored, rework to check the real directive names..
  - Grade D, tier `experimental`, weight 0 — target tier met. Landed as specified: the invented `ai-content-declaration` name stops being the pass condition and is reported as invented, the AIPREF `Content-Usage` attachment (response header or robots.txt rule) is the pass, and `noai`/`noimageai` warn with the "no AI vendor documents honoring these names" caveat in the message text. The false GPTBot/ClaudeBot claim is deleted with a regression test pinning it, absence is `na` instead of a guaranteed medium-priority fail on every site, all pages are scanned rather than `pages[0]`, and the crude URL validation is gone (no spec defines a valid value). `tdm-reservation` is deliberately left to `tdm-rep` so one adoption decision is not priced twice; `defaultPriority` `medium` → `low`.
- [x] DONE `answer-readiness/direct-definitions` — rewritten to an intent-gated, language-neutral check 2026-08-22 (Plan 4, Task 16) · [dossier](../../../../docs/evidence/audits/answer-readiness/direct-definitions.md)
  - Rework detector: language-neutral structural signals (dfn/dl semantics, first-sentence definition patterns per detected language), notApplicable when page has no definitional intent..
  - Landed at tier `informative`, not `scored` — the graded evidence is **C**, and `weightForGrade('C', 'scored') = 0` while `sunset.test.ts` enforces `tier !== 'scored' ⟺ weight === 0`, so grade C in the `scored` tier is not a registrable state; the `scored` target was a proposal pending triage, and the same resolution was recorded for `openapi-link`. Grade C, weight 0. The detector rework landed in full: the bold-colon branch (graded D on its own, and satisfied by every `<strong>Note:</strong>`) is deleted along with its first-occurrence parent-text slicing, the leaky `isArticleContentPage` gate is replaced by a definitional-intent gate that is structural first (`<dfn>`/`<dl>` in any language) and lexical second (per-language "what is X?"/glossary patterns for 11 languages), substantiveness is measured in characters as well as words so CJK definitions finally count, prose definitions are detected via each language's copula pattern and reported instead of failed, and `na` covers a crawl with no definitional page. The audit can no longer fail; `defaultPriority` `medium` → `low`.
- [x] DONE `answer-readiness/meta-description-aeo` (9.11) — redeemed by merge into `answer-readiness/meta-description` 2026-08-22 (Plan 4, Task 6) · [merged dossier](../../../../docs/evidence/audits/answer-readiness/meta-description.md) · [source dossier](../../../../docs/evidence/merged/answer-readiness/meta-description-aeo.md)
  - Redeem via merge into meta-description: one audit, quality criteria without the invented 'AEO formula'..
- [x] DONE `access-crawl-control/bytespider` (2.9) — consolidated into `access-crawl-control/ai-bot-directives` 2026-08-22 (Plan 4, Task 2) · [merged dossier](../../../../docs/evidence/audits/access-crawl-control/ai-bot-directives.md) · [source dossier](../../../../docs/evidence/merged/access-crawl-control/bytespider.md)
  - Consolidate all low-signal per-bot audits into one 'ai-bot-directives' audit: parse robots.txt once, informational per-bot table, score only on documented-active bots..
- [x] DONE `access-crawl-control/cohere-ai` (2.10) — consolidated into `access-crawl-control/ai-bot-directives` 2026-08-22 (Plan 4, Task 2) · [merged dossier](../../../../docs/evidence/audits/access-crawl-control/ai-bot-directives.md) · [source dossier](../../../../docs/evidence/merged/access-crawl-control/cohere-ai.md)
  - Same consolidation into ai-bot-directives..
- [x] DONE `access-crawl-control/youbot` (2.11) — consolidated into `access-crawl-control/ai-bot-directives` 2026-08-22 (Plan 4, Task 2) · [merged dossier](../../../../docs/evidence/audits/access-crawl-control/ai-bot-directives.md) · [source dossier](../../../../docs/evidence/merged/access-crawl-control/youbot.md)
  - Same consolidation into ai-bot-directives..
- [x] DONE `access-crawl-control/diffbot` (2.12) — consolidated into `access-crawl-control/ai-bot-directives` 2026-08-22 (Plan 4, Task 2) · [merged dossier](../../../../docs/evidence/audits/access-crawl-control/ai-bot-directives.md) · [source dossier](../../../../docs/evidence/merged/access-crawl-control/diffbot.md)
  - Same consolidation into ai-bot-directives..
- [x] DONE `access-crawl-control/ai2bot` (2.13) — consolidated into `access-crawl-control/ai-bot-directives` 2026-08-22 (Plan 4, Task 2) · [merged dossier](../../../../docs/evidence/audits/access-crawl-control/ai-bot-directives.md) · [source dossier](../../../../docs/evidence/merged/access-crawl-control/ai2bot.md)
  - Same consolidation into ai-bot-directives..
- [x] DONE `access-crawl-control/tdm-rep` — internal incoherence fixed, moved to experimental 2026-08-22 (Plan 4, Task 16) · [dossier](../../../../docs/evidence/audits/access-crawl-control/tdm-rep.md)
  - TDM Reservation Protocol is a real W3C CG spec with EU AI Act relevance. Experimental flag, unscored, fix internal incoherence..
  - Grade C, tier `informative` → `experimental`, weight 0 — target tier met. Every item on the required-fix list landed: reservation `1` (denied) and `0` (permitted) are distinct outcomes instead of a shared pass; anything that is not 1 or 0 is reported as undefined by the protocol rather than narrated as an affirmative grant of mining rights; absence is `na`, so the whole web stops losing 0.5 for not adopting a convention nothing consumes; `JSON.parse` is gated on media type **and** a leading-`<` guard, so an SPA soft-404 reads as "no file" instead of "malformed policy"; the file is validated against the spec's array-of-objects shape, so `123` and `{"foo":1}` are rejected; meta declarations are collected across every page and disagreement warns instead of first-match-wins. Added the `tdm-reservation` response header (the CG's preferred technique, previously ignored) and removed the dead `ctx.fetch` fallback, so the audit is synchronous and issues no request. `defaultPriority` `medium` → `low`. Deviation: the required fix's "move to a compliance category" was **not** done — an audit id is `category/slug`, so that renames the id and breaks migration-map; the tier and weight 0 achieve the same isolation from the score.
- [x] DONE `answer-readiness/twitter-card` (4.10) — redeemed by merge into `answer-readiness/core-open-graph` 2026-08-22 (Plan 4, Task 6) · [merged dossier](../../../../docs/evidence/audits/answer-readiness/core-open-graph.md) · [source dossier](../../../../docs/evidence/merged/answer-readiness/twitter-card.md)
  - Fix factual errors (twitter:* falls back to og:_), fold into social-meta diagnostic with core-open-graph, unscored. Evidence: og:title/og:site_name graded A; twitter:_ has no AI consumer evidence..
- [x] DONE `agent-interfaces/openapi-link` (4.18) — redeemed by merge into `agent-interfaces/openapi-exists` 2026-08-22 (Plan 4, Task 7) · [merged dossier](../../../../docs/evidence/audits/agent-interfaces/openapi-exists.md) · [source dossier](../../../../docs/evidence/merged/agent-interfaces/openapi-link.md)
  - Redeem via merge into agent-interfaces/openapi-exists: one discovery audit for real mechanisms incl. RFC 9727 api-catalog (graded B), drop link-tag requirement that fails every site.. Landed at tier `informative`, not `scored` — the RFC 9727 evidence names `informative` as its own recommended tier ("until a consumer is documented"), and the `scored` target was a proposal pending triage.
- [x] DONE `agent-interfaces/cors-api-routes` — rebuilt spec-driven, `na` without an API surface 2026-08-22 (Plan 4, Task 16) · [dossier](../../../../docs/evidence/audits/agent-interfaces/cors-api-routes.md)
  - Keep scored but notApplicable unless site exposes a public API surface agents would call cross-origin..
  - Landed at tier `informative`, not `scored` — the evidence signal is graded **C** with a recommended tier of `informative` and an explicit instruction to demote it out of the score; `weightForGrade('C', 'scored') = 0` while `sunset.test.ts` enforces `tier !== 'scored' ⟺ weight === 0`, so grade C in the `scored` tier is not a registrable state (same resolution as `openapi-link`). Grade C, weight 0. The substantive half of the row landed in full: a site publishing no OpenAPI document is `na` instead of losing half a point, and probing is now spec-driven — `servers[].url` resolved against the site base with `{variable}` defaults expanded, crossed with the declared **concrete** paths (templated ones skipped so a `{id}` 404 is not read as missing CORS), capped at two endpoints, OPTIONS with a GET fallback, and every target `isSafeUrl()`-gated because `servers[].url` is site-controlled. An ACAO must be `*` to pass: a single named origin and a same-origin-only value both warn, where presence alone used to pass. The dead `/swagger.json` gate arm is dropped, `/openapi.json` is soft-404 gated, and the false "blocks all agentic workflows / MCP clients" framing is replaced with the one consumer class the evidence supports — browser-sandboxed agent code such as an OpenAI Apps SDK widget. The audit can no longer fail; `defaultPriority` `medium` → `low`.
